# Pi10：Pi Native 投资助手最终收口计划与实施报告

> 日期：2026-09-14
> 目标：在 Pi7/Pi8/Pi9 已完成的 Package 化基础上，完成 root runtime 辅助模块收口、全局架构门禁强化、真实投研闭环验证，并以可审计证据判断是否达到 Pi Native 完成定义。

## 一、当前真实基线

数字由 `bun run report:pi7` 生成，不手工维护：

```text
workspacePackages: 49
piNativePackages: 41
rootSourceFiles: 71
rootProductionFiles: 43
rootProductionLines: 7598
legacyEventConsumers: 0
globalRegistryConsumers: 0
agentSessionFactories: 1
```

已完成的本轮收口：

- 新增 `@upup/pi-prompt-config`，承接 prompt、locale、channel、feature gate、investment config、capability manifest、role system。
- `@upup/pi-resource-composition` 承接 package discovery、tool ownership、plugin adapter、skill command discovery。
- `@upup/pi-event-adapter` 承接 event stream，root 仅保留 runner 注入 bridge。
- 删除 root MCP facade、旧 command registry、root prompt service；prompt runner 通过显式 `PiPromptPort` 接入。
- `default-prompt.ts` inline；无生产消费者的 `investment-subagents.ts` 删除。

## 二、剩余 root runtime 迁移矩阵

| Root 模块 | 当前 | 目标 | 收口条件 |
|---|---|---|---|
| `agent-session-factory.ts` | 唯一 Session composition root | 保留为唯一 root factory | 只组合 Package catalog/capability，不新增业务分支 |
| `runner.ts` | root Pi session runner | 保留为唯一 runner | 所有入口通过此 runner |
| `agent-spec.ts` | root profile/spec 定义 | `@upup/pi-investment-workflow` 或 runtime contract | profiles 可序列化且无重复 registry |
| `registry.ts` / `agent-catalog.ts` | root agent catalog | `@upup/pi-runtime` 或 workflow package | 无第二套 AgentDefinition store |
| `agent-port.ts` | root port 类型/显式 runtime port | `@upup/pi-runtime` contract | 无 globalThis fallback |
| `tool.ts` | root PiTool 兼容类型 | `@upup/pi-runtime` contract | 无独立 tool registry |
| `prompts.ts` | root prompt assembly | `@upup/pi-app` / prompt package | 只注入 Package resources 与 session context |
| `investment-workflow.ts` | 87 行 root factory bridge | `@upup/pi-app` composition | Package 不反向 import root |
| `bootstrap.ts` | 入口 bootstrap | `@upup/pi-app` | CLI/stdio/gateway/bridge 共享 bootstrap |
| `index.ts` | root compatibility barrel | 删除或只保留公开 runtime contract | 生产消费者为零后删除 |

## 三、阶段计划

### 阶段 A：修复验证缺口

1. 修复并稳定复现 `packages/pi-bridge/src/session-sync.e2e.test.ts` 的 WebSocket timeout/race。
2. 为 `PiPromptPort` 增加 isolation、未 bootstrap fail-closed、重复 bootstrap 幂等测试。
3. 为 `@upup/pi-event-adapter` 的 `streamPiAgentWithRunner` 增加 injected-runner contract test。
4. 更新门禁，检查 Package → root `src` 反向依赖和所有新增动态 import。

### 阶段 B：runtime contract 下沉

1. 将 `agent-port.ts` 的公共 port 类型下沉到 `@upup/pi-runtime`，root 仅保留 wiring。
2. 将 `tool.ts` 的公共 PiTool contract 下沉到 `@upup/pi-runtime`，删除 root compatibility implementation。
3. 将 `agent-catalog.ts` 与 `registry.ts` 的可序列化 catalog 能力下沉到既有 runtime/workflow package；删除重复 store。
4. 评估 `agent-spec.ts` 的 Profile 定义归属，确保 `@upup/pi-investment-workflow` 是投资 profile 的唯一 owner。

### 阶段 C：入口与资源最终收口

1. 将 prompt assembly 与 workflow factory bridge 收口到 `@upup/pi-app`（仅在明确边界成立时新增 package）。
2. 将 root `src/runtime/pi/index.ts` 改为最小 public contract barrel，删除无消费者 re-export。
3. 将 `src/evals`、`src/management`、`src/print.ts` 改为 Package API + root bootstrap，不直接读取 runtime 私有模块。
4. 强化 root allowlist 静态检查：仅允许 `src/index.tsx`、`src/cli.ts`、`src/compat/**`、`src/bootstrap/**` 与登记的必要 migration/bridge。

### 阶段 D：完整验证与产品验收

```text
bun run typecheck
bun run check:module-boundaries
bun run check:pi-migration
bun run check:pi-packages
bun run check:pi-runtime
bun run check:pi7
bun run test:pi-contracts
bun test <changed packages and entry contracts>
bun test
bun run build
bun run start -- --help
```

产品验收必须分开记录：

- fixture：A 股、港股、海外市场，detect → plan → execute → verify → report，恢复、证据、审计、风险和导出。
- provider：仅在配置凭证时运行 `/invest NVDA 估值`，单独记录 provider、模型、时间、凭证来源和结果；不得用 fixture 结果替代真实 provider 证据。
- 并发恢复：多 Session、abort、compact、restart、provider failure、retry、dispose。

## 四、完成定义

只有同时满足以下条件，才能把 Pi10/Pi7 标记为完成：

- 唯一 `createAgentSession`/AgentSession factory。
- 所有能力通过 Pi Package manifest、extension、capability context 接入。
- 无生产 `legacy-events`、globalThis registry、重复 tool/skill/agent registry。
- root `src` 无金融业务、工具实现、技能 loader、MCP 实现或独立 Agent loop。
- CLI、Gateway、Bridge、stdio、Cron、Daemon、SDK、Eval 共用 Pi Runtime。
- `/invest` 五阶段可恢复、证据可追溯、风险可审计、副作用默认 sandbox/deny/approval。
- 静态门禁、Package contract、全仓测试、构建和所有入口 smoke 通过。
- 真实 provider 结果与本地 fixture 结果分开记录。

当前判断：核心架构已达到 99.95% 级别；尚未达到 100%，原因是 session-sync race、root runtime contract 仍有少量核心实现、真实 provider smoke 缺少凭证证据。

## 五、本轮执行结果（2026-09-14）

已完成阶段 A 的主要工作：

- 修复 `session-sync` WebSocket 首帧/持久化竞态；定向测试连续 5 次通过。
- `PiPromptPort` contract 增加注册、解析、reset 隔离测试：16 pass / 0 fail。
- 注入式 event stream contract 测试：`@upup/pi-event-adapter` 60 pass / 0 fail。
- 更新 `check:pi-packages` 读取迁移后的 ownership source；过滤 dist 测试源码。
- 最终全仓 `bun test`：2229 pass / 0 fail / 6857 assertions / 211 files。
- 最终门禁、`test:pi-contracts`、构建和 `start -- --help` 均通过。

当前未完成项仅限：

1. `OPENAI_API_KEY`、`FINANCIAL_DATASETS_API_KEY` 未配置，真实 `/invest NVDA 估值` provider smoke 暂无法执行。
2. `agent-spec`、`registry`、`agent-catalog`、`agent-port`、`tool`、`runner`、`agent-session-factory` 是必要 root composition 核心，不应为减少文件数而复制或错误拆分；后续只做 contract 收口。
3. `investment-workflow.ts` 是唯一 root factory injection bridge，保留至 `@upup/pi-app` 边界明确后再迁移。

## 六、Pi81 真实基线同步（2026-09-15）

当前自动结构报告：`48` workspace packages、`48/48` Pi-native manifests、root production files `0`、root production lines `109`、唯一 production `createAgentSession`、legacy/global consumers `0`、native tool coverage `269/269 = 100%`。副作用 manifest 覆盖为 `27/27 = 100%`，并由运行时 Session smoke 验证文件、凭证、网络、金融四类 policy audit。

本轮新增 `verify:pi-side-effects` 并接入 `test:pi-contracts`；`@upup/pi-runtime` 与 `@upup/pi-session` 独立构建的 `financial-datasets` provider 类型缺口已修复。当前仍不能标记 Pi10/Pi7 完成：真实 provider dossier、真实 provider retry/recovery、跨入口真实 Session 证据及生产级 approval/audit 证据尚未在用户授权凭证环境执行。

## 七、Pi83 runtime 收口同步（2026-09-15）

`@upup/pi-prompt-config` 现在是默认投资 system prompt、能力说明和 Coach prompt 的唯一 prompt-side owner；`@upup/pi-session` 通过 Package public API 组合，不再硬编码默认身份文本。Session prompt runner 的 AgentSpec validation 已使用 `@upup/pi-runtime` contract。Pi7 静态结构保持 `48/48` manifest、`269/269` native tools、root production files `0`、唯一 Session factory；本轮全仓 `2175 pass`。真实 provider 与生产授权审计仍未执行，因此不标记 Pi7/Pi10 完成。

## 八、Pi84 门禁同步（2026-09-15）

Prompt ownership 已纳入 production entry contract 和 Pi7 architecture gate：默认 prompt 由 `@upup/pi-prompt-config` 唯一持有，AgentSpec validation 由 `@upup/pi-runtime` 持有。当前结构报告仍为 `48/48` manifest、`269/269` native tools、root production files `0`、唯一 Session factory；真实 provider 和 production approval/audit 证据仍是 Pi10/Pi7 的未完成硬门槛。

## 九、Pi85 composition boundary 同步（2026-09-15）

`@upup/pi-session` 现通过 `builtin-composition.ts` 单一边界消费金融、平台、market-data 和 Cron composition，Factory 不直接依赖具体业务 composition 实现。结构报告保持 `48/48` manifest、`269/269` native tools、root production files `0`、唯一 Session factory；全仓 `2177 pass`。真实 provider 与授权 production audit 仍未完成。

## 十、Pi86 composition provider 同步（2026-09-15）

`@upup/pi-session` 已公开 `PiSessionCompositionProviders` contract，Session Factory 支持注入可替换的 Finance/Platform/market-data/Cron composition，默认使用 builtin provider。全仓 `2177 pass`，结构指标保持 48/48 manifest、269/269 native tools、root production files 0；真实 provider 与授权 production audit 仍未完成。
