# LLM API Keys
OPENAI_API_KEY=your-openai-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key
# Pi resolves Google credentials from GEMINI_API_KEY.
GEMINI_API_KEY=your-google-api-key
# Legacy name, still accepted as a fallback.
GOOGLE_API_KEY=your-google-api-key
XAI_API_KEY=your-xai-api-key
OPENROUTER_API_KEY=your-openrouter-api-key
MOONSHOT_API_KEY=your-moonshot-api-key
DEEPSEEK_API_KEY=your-deepseek-api-key

# MiniMax (global + China)
MINIMAX_API_KEY=your-MiniMax-api-key
MINIMAX_CN_API_KEY=your-MiniMax-cn-api-key

# Ollama (Local LLM)
# UpUp registers Ollama into Pi as an OpenAI-compatible provider (keyless);
# the /v1 suffix is appended automatically, so point this at the Ollama root.
OLLAMA_BASE_URL=http://127.0.0.1:11434

# Persistent memory embeddings reuse existing model API keys.
# Priority: OpenAI (OPENAI_API_KEY) -> Gemini (GEMINI_API_KEY, or legacy GOOGLE_API_KEY) -> Ollama (OLLAMA_BASE_URL)
# No additional memory-specific API keys required.

# Stock Market API Key
FINANCIAL_DATASETS_API_KEY=your-financial-datasets-api-key

# Web Search API Keys (Exa → Perplexity → Tavily)
EXASEARCH_API_KEY=your-exa-api-key
PERPLEXITY_API_KEY=your-perplexity-api-key
TAVILY_API_KEY=your-tavily-api-key

# X/Twitter API (enables x_search tool for public sentiment research)
X_BEARER_TOKEN=your-X-bearer-token

# Pi telemetry is local by default. Configure an external sink through
# UpUp telemetry settings when required.

# Optional Pi Package loading. Packages are never auto-discovered: configure
# all three values explicitly and pin every package to an exact semver.
# UPUP_PI_PACKAGE_PATHS=["/absolute/path/to/@upup/pi-finance-sdk"]
# UPUP_PI_TRUSTED_PATHS=["/absolute/path/to"]
# UPUP_PI_PINNED_PACKAGES={"@upup/pi-finance-sdk":"0.1.0"}
