- **验证**：`bun test src/gateway/gateway-sla-runner.test.ts` 为 `2 pass / 0 fail`、`4 expect() calls`；`typecheck`、`check:pi-packages`、`check:pi-migration`、`check:module-boundaries` 全部通过；`bun run verify:pi5` A1–A20 `20/20`；`bun test` 全仓为 3179 pass / 0 fail、`10749 expect() calls`。
