# Plan P3: 性能优化与新功能规划 (v1.0)

> 基于 p2.md 完成的 pi-tui 改造，制定后续优化计划

**版本**: v1.1
**创建日期**: 2026-05-21
**更新日期**: 2026-05-21
**状态**: 🚧 进行中
**基于**: p2.md 完成 (pi-tui 0.73.1 全面改造)

---

## 交互验证结果 (v1.1)

### 验证命令
```bash
bun run dev      # 开发模式
bun run build    # 构建
bun run start    # 启动
./dist/upup      # 编译后执行
```

### 验证结果
| 检查项 | 结果 |
|--------|------|
| bun run dev | ✅ 启动成功 |
| bun run build | ✅ 构建成功 (3073 modules) |
| bun run start | ✅ 启动成功，显示欢迎界面 |
| ./dist/upup --help | ✅ 帮助信息正常 |
| bun test | ✅ 2615 pass, 0 fail |
| pi-tui 组件 | ✅ BorderBox, Markdown, Box 正常工作 |

### 启动界面截图描述
```
╔══════════════════════════════════════════════════════════════╗
║   Welcome to UpUp v2026.05.15                               ║
╚══════════════════════════════════════════════════════════════╝
 ██╗   ██╗ ██████╗  ██╗   ██╗ ██████╗
 ██║   ██║ ██╔══██╗ ██║   ██║ ██╔══██╗
...
Model: DeepSeek V4 Flash
────────────────────────────────────────────────────────────────
 / for commands
```

---

## 一、代码库分析

### 1.1 项目统计

| 指标 | 数值 |
|------|------|
| TypeScript 文件总数 | ~600 |
| 代码行数 | 136,521 |
| 最大文件 | cli.ts (48KB) |
| 最大 agent 文件 | agent.ts (1229 行) |

### 1.2 待优化文件

| 文件 | 行数 | 优化方向 |
|------|------|----------|
| src/agent/agent.ts | 1229 | 模块拆分 |
| src/agent/subagent-runner.ts | 591 | 职责分离 |
| src/controllers/agent-runner.ts | 561 | 职责分离 |
| src/agent/scratchpad.ts | 557 | 重构 |
| src/agent/loop-recovery.ts | 502 | 测试覆盖 |

---

## 二、性能优化计划

### 2.1 缓存系统升级 (P1)

**目标**: 提升缓存效率，减少重复计算

**任务**:
- [ ] 实现 LRU 缓存替代简单 TTL
- [ ] 实现后台刷新机制
- [ ] 添加请求去重 (deduplication)
- [ ] 优化 Memory Manager 缓存

**文件**: `src/utils/cache.ts` (新建)

```typescript
export class LRUCache<K, V> {
  constructor(maxSize: number)
  get(key: K): V | undefined
  set(key: K, value: V): void
  clear(): void
}

export function memoizeWithBackgroundRefresh<T>(
  fetcher: () => Promise<T>,
  ttlMs: number
): () => Promise<T>
```

### 2.2 Agent 模块拆分 (P1)

**目标**: 减少 agent.ts 文件大小，提升可维护性

**问题**: agent.ts 1229 行，职责过重

**拆分方案**:
```
src/agent/
├── agent.ts           # 主协调器 (→ 精简到 ~400 行)
├── agent-core.ts      # 核心循环逻辑
├── agent-hooks.ts     # Hook 集成
├── agent-state.ts     # 状态管理
└── agent-tools.ts     # 工具注册
```

**任务**:
- [ ] 创建 agent-core.ts 提取核心循环
- [ ] 创建 agent-hooks.ts 管理 Hook 生命周期
- [ ] 创建 agent-state.ts 管理状态
- [ ] 精简 agent.ts 到 ~400 行

### 2.3 内存优化 (P2)

**目标**: 减少内存占用，提升响应速度

**任务**:
- [ ] 实现消息压缩 (compaction)
- [ ] 优化 Token 计数
- [ ] 添加内存监控
- [ ] 实现流式响应优化

**当前 Compact 逻辑** (`src/agent/compact.ts`):
- COMPACTION_THRESHOLD = 0.8
- MIN_TOOL_RESULTS = 3

**优化方向**:
- 增量压缩而非全量
- Token 预算动态调整
- 压缩前缓存关键信息

---

## 三、新功能计划

### 3.1 高级权限管理 (P1)

**目标**: 完善权限系统的企业级功能

**任务**:
- [ ] MCP 工具规则支持
- [ ] 企业级托管策略
- [ ] 敏感路径保护 (.git/, .claude/)
- [ ] 权限规则导入/导出

**配置文件格式**:
```json
{
  "permissions": {
    "mcpServers": {
      "filesystem": {
        "allowedPaths": ["/home/user/*"],
        "deniedPaths": ["**/.ssh/**"]
      }
    }
  }
}
```

### 3.2 自动化测试增强 (P2)

**目标**: 提升测试覆盖率和可靠性

**任务**:
- [ ] 扩展权限系统测试
- [ ] 添加 MCP 服务器测试
- [ ] 实现集成测试
- [ ] 添加性能基准测试

**测试覆盖目标**:
- 当前: ~40%
- 目标: ~70%

### 3.3 用户体验增强 (P2)

**目标**: 改善 CLI 交互体验

**任务**:
- [ ] 权限模式状态栏指示器
- [ ] Slash 命令 `/dangerously` `/safemode`
- [ ] 进度条实时更新
- [ ] 命令补全增强

**UI 示例**:
```
[UpUp] ⚡ bypassPermissions | Model: claude-sonnet-4 | Session: 2h 15m
```

---

## 四、技术债务清理

### 4.1 类型系统增强 (P2)

**任务**:
- [ ] 添加泛型约束到工具系统
- [ ] 创建错误类层次
- [ ] 添加类型守卫函数

**错误类层次**:
```typescript
class UpupError extends Error {
  constructor(message: string, public code?: string) {
    super(message)
    this.name = 'UpupError'
  }
}

class PermissionError extends UpupError {
  constructor(message: string, public toolName: string) {
    super(message, 'PERMISSION_DENIED')
  }
}

class TimeoutError extends UpupError {
  constructor(message: string, public timeoutMs: number) {
    super(message, 'TIMEOUT')
  }
}
```

### 4.2 统一常量管理 (P3)

**任务**:
- [ ] 创建 `src/constants/index.ts`
- [ ] 提取魔法数字
- [ ] 统一配置常量

```typescript
export const AGENT = {
  DEFAULT_MAX_ITERATIONS: 50,
  MAX_OVERFLOW_RETRIES: 2,
  OVERFLOW_KEEP_ROUNDS: 3,
} as const

export const TOOLS = {
  APPROVAL_REQUIRED: ['write_file', 'edit_file'] as const,
  DEFAULT_TIMEOUT_MS: 30000,
} as const
```

---

## 五、实施计划

### 5.1 时间表

| 阶段 | 任务 | 优先级 | 预计时间 |
|------|------|--------|----------|
| P1 | 缓存系统升级 | 高 | 4h |
| P1 | Agent 模块拆分 | 高 | 6h |
| P1 | 高级权限管理 | 高 | 4h |
| P2 | 内存优化 | 中 | 4h |
| P2 | 自动化测试 | 中 | 8h |
| P2 | 用户体验增强 | 中 | 4h |
| P2 | 类型系统增强 | 中 | 4h |
| P3 | 常量管理 | 低 | 2h |

**总计**: ~36 小时

### 5.2 里程碑

| 里程碑 | 任务 | 目标日期 |
|--------|------|----------|
| M1 | 缓存系统 + Agent 拆分 | 2026-05-22 |
| M2 | 高级权限管理 | 2026-05-23 |
| M3 | 测试覆盖达标 | 2026-05-24 |
| M4 | 类型系统增强 | 2026-05-25 |

---

## 六、验收标准

### 6.1 性能指标

- [ ] 缓存命中率 > 80%
- [ ] Agent 循环启动时间 < 500ms
- [ ] 内存占用减少 15%

### 6.2 代码质量

- [ ] Agent.ts 减少到 ~400 行
- [ ] 测试覆盖率 > 70%
- [ ] 无类型错误
- [ ] 无 lint 警告

### 6.3 功能验收

- [ ] MCP 工具规则支持
- [ ] 权限模式指示器
- [ ] 错误类层次完整

---

## 七、实施状态 (v1.1)

### 已完成
- ✅ p2.md 全部完成
- ✅ pi-tui 组件集成
- ✅ 权限系统实现
- ✅ keybindings 类型修复
- ✅ 交互验证通过

### 待实施
- 🔲 缓存系统升级
- 🔲 Agent 模块拆分
- 🔲 高级权限管理
- 🔲 内存优化
- 🔲 测试覆盖率提升

### 验证命令
```bash
bun run build   # 构建验证
bun run start   # 交互验证
bun test        # 测试验证
./dist/upup --help  # CLI 验证
```

---

## 七、风险评估

| 任务 | 风险 | 影响 | 缓解措施 |
|------|------|------|----------|
| Agent 模块拆分 | 中 | 高 | 逐步拆分，保持功能测试 |
| 缓存系统升级 | 低 | 中 | 保留旧缓存作为 fallback |
| 类型系统增强 | 低 | 中 | 使用渐进式迁移 |

---

## 八、后续计划

### 8.1 P4 规划 (2026-05-26+)

- 国际化支持 (i18n)
- 主题系统增强
- 插件系统

### 8.2 长期目标

- 企业级功能
- 云端同步
- API 开放

---

**计划制定者**: Claude Code
**版本**: v1.0
**创建日期**: 2026-05-21