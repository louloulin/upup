{
  "name": "@upup/agent-core",
  "version": "0.2.0",
  "description": "UpUp compatibility DTOs for the Pi-backed runtime; not an agent executor",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --target=bun --external:playwright --external:playwright-core --external:electron && bun x tsc --emitDeclarationOnly --declaration --declarationMap --outDir dist",
    "dev": "bun build src/index.ts --outdir=dist --watch --target=bun",
    "test": "bun test"
  },
  "dependencies": {
    "@upup/types": "workspace:*"
  },
  "files": [
    "dist",
    "src"
  ],
  "private": false
}
