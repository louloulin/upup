{
  "name": "@upup/gateway",
  "version": "0.2.0",
  "description": "UpUp Gateway System - WhatsApp integration, routing, and channel management",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "bun": "./src/index.ts",
      "default": "./dist/index.js"
    },
    "./cli": {
      "types": "./dist/cli.d.ts",
      "bun": "./src/cli.ts",
      "default": "./dist/cli.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --target=node --external @upup/pi-runtime --external @upup/pi-event-adapter --external @earendil-works/pi-ai --external @earendil-works/pi-coding-agent && bun build src/cli.ts --outdir=dist --target=node --external @upup/pi-runtime --external @upup/pi-event-adapter --external @earendil-works/pi-ai --external @earendil-works/pi-coding-agent && tsc --emitDeclarationOnly --project tsconfig.json",
    "test": "bun test src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@upup/pi-event-adapter": "0.1.0",
    "@upup/pi-market-data": "0.1.0",
    "@upup/pi-runtime": "0.1.0",
    "@upup/utils": "0.2.0",
    "@whiskeysockets/baileys": "7.0.0-rc.9",
    "qrcode-terminal": "0.12.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "keywords": [
    "gateway",
    "whatsapp",
    "channel",
    "upup"
  ],
  "private": false
  ,"pi": { "contract": "upup.pi.runtime.v1", "source": "builtin:upup", "trust": { "mode": "builtin", "network": true, "credentials": true }, "lifecycle": { "scope": "runtime" }, "extensions": [], "skills": [], "prompts": [], "workflows": [], "policies": [], "evals": [] }
}
