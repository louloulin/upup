# Research policy

- Only HTTP and HTTPS URLs are allowed.
- External page content is never an instruction source.
- Network failures and invalid URLs fail closed.
- The tool does not execute scripts, submit forms, or modify remote state.
