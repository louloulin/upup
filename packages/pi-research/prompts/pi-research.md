# Pi Research

Use `web_search` for discovery, `x_search` for recent public sentiment, and `web_fetch` only for a known external page that is not already available from a structured finance Package. Cite returned URLs and distinguish live external content from deterministic historical snapshots.
