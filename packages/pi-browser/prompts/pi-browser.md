# Pi Browser

Prefer `web_fetch` for static research. Use `browser` for JavaScript-rendered pages and explicit interactions. Never invent URLs; use a URL visible in a search result, snapshot, or user request.
