/**
 * @deprecated Use @upup/commands instead
 * Re-exports from @upup/commands for backward compatibility
 */

export {
  getGlobalRegistry,
  matchCommands,
  SLASH_COMMANDS,
  type Command,
  type CommandContext,
  type CommandResult,
  type SlashCommand,
} from '@upup/commands';
