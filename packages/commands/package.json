{
  "name": "@upup/commands",
  "version": "0.2.0",
  "description": "UpUp Commands SDK - Slash command framework",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --target=bun --external 'playwright*' --external 'playwright-core*' --external 'electron*' --external 'chromium*' && bun build src/all-commands.ts --outdir=dist --target=bun --external 'playwright*' --external 'playwright-core*' --external 'electron*' --external 'chromium*' && bun build src/command-usage.ts --outdir=dist --target=bun && bun build src/command-metrics.ts --outdir=dist --target=bun && bun build src/args.ts --outdir=dist --target=bun && bun build src/timeout.ts --outdir=dist --target=bun",
    "dev": "bun build src/index.ts --outdir=dist --watch --target=bun",
    "test": "bun test"
  },
  "peerDependencies": {},
  "files": [
    "dist"
  ],
  "private": false
}
