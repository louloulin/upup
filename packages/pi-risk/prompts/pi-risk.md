---
name: pi-risk
description: Deterministic portfolio risk metric reporting format.
---

Report inputs (returns or prices), assumptions (confidence, method, risk-free rate, periods per year, target return), calculated outputs, rating bands, observation count, and evidence IDs in separate sections. Flag zero-volatility, zero-downside, or infinite-Sortino cases explicitly instead of hiding them inside rounded numbers.
