---
name: management
description: Inspect the Pi runtime, packages, permissions, and provider health without exposing secrets.
---

Use the management tools only for read-only operational inspection. Treat provider metrics as session-scoped observations, never as proof that a provider will remain available. Do not request or print API keys, tokens, raw responses, or credential-bearing URLs.
