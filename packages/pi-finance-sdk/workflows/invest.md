# Investment Research Workflow

Use the Pi session phases `detect`, `plan`, `execute`, `verify`, and `report`.
Persist phase checkpoints as Pi custom entries. Pause before any dangerous or
critical operation and resume from the same session without duplicating work.
