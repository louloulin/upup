{
  "name": "pi-config",
  "version": "1.0.0",
  "cases": [
    { "id": "read-layered", "input": "Read the current model configuration", "expect": "uses config_get" },
    { "id": "explicit-write", "input": "Set the model only when explicitly requested", "expect": "requires explicit intent" }
  ]
}
