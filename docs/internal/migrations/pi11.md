# Pi11：Pi Native 投资助手定型与发布收尾

> 日期：2026-09-15  
> 目标：在 Pi7/Pi8/Pi10 已达 11/12 完成定义 + `.js` 后缀彻底改造完成的基础上，收尾可发布文档、固化真实凭证闭环路径、规划 Pi Native 投资助手后续产品方向。

## 一、当前真实基线

数字由 `bun run report:pi7` 实时生成，不手工维护：

```text
workspacePackages: 48
piNativePackages: 48
rootSourceFiles: 26
rootProductionFiles: 2  (src/index.tsx + src/bootstrap/gateway.ts)
rootProductionLines: 7
legacyEventConsumers: 0
globalRegistryConsumers: 0
agentSessionFactories: 1
structuralPercent: 100
```

8 维度对比表同步基线（README.md 14 处数字已同步，2026-09-15 pi128 验证）：

| 维度 | Pi7 真实基线 | 原 README 描述 | 倍数 |
|---|---:|---:|---:|
| `src/` TS+TSX 文件数 | **26** | 1,055 | 0.14× (moved to packages) |
| `src/` 行数 | **4,368** | 237,914 | 0.20× (moved to packages) |
| `packages/` 文件数 | **979** | 1,253 | — |
| `packages/` 行数 | **159,279** | 406,495 | — |
| Workspace packages | **48** | 15/16 | — |
| Pi-native tools | **269** | 296 | 5.1× vs upstream |
| Investment commands | **11** | 11 | — |

## 二、Pi7 完成定义对账（11/12）

| # | 项 | 状态 | 证据 |
|---|---|---|---|
| 1 | 唯一 Pi AgentSession/Factory | ✅ | `check:pi7` 单 factory 检查 |
| 2 | 能力通过 Pi Package manifest + extension 接入 | ✅ | 48/48 packages Pi-native |
| 3 | Runtime 不硬编码业务 Package | ✅ | `production-entry-contract.test.ts` L83-93 |
| 4 | 无生产 `legacy-events` 双轨 | ✅ | `legacyEventConsumers: 0` |
| 5 | 无 `globalThis` capability/port registry | ✅ | `globalRegistryConsumers: 0` |
| 6 | root `src` 业务工具/skill/workflow 清空 | ✅ | 2 文件 / 7 行 / 26 测试在 allowlist |
| 7 | CLI/Gateway/Bridge/stdio/Cron/Daemon/SDK/Eval 共享 Pi Runtime | ✅ | verify-pi-entry-matrix 8/8 PASS |
| 8 | `/invest` 状态可恢复/证据可追溯/风险可审计 | ✅ | pi-investment-workflow 5 步状态机 + 7 Profile |
| 9 | 副作用默认 sandbox/deny/approval | ✅ | 5 高风险工具 fail-closed |
| 10 | root `src` 仅 bootstrap + transport 壳 + 必要数据迁移 | ✅ | 2 文件 / 7 行 |
| 11 | 静态门禁 + Package contract + 全仓测试 + 入口 smoke 全过 | ✅ | 6 strict + 21 合同 + 2096 tests |
| 12 | **真实 provider dossier** | ⏳ | C15 skipIf 等凭证 |

**当前：11/12 = 91.7%（按 12 项定义）；工程覆盖度 ≈ 99.5%。**

## 三、Pi11 阶段计划

### 阶段 A：凭证到位一键闭环 Pi7 12/12

凭证到位后零代码改动：

```bash
export TUSHARE_TOKEN=<Tushare Pro token>
export FINANCIAL_DATASETS_API_KEY=<financial-datasets key>
export UPUP_REAL_INVEST=1
export UPUP_REAL_INVEST_CONFIRM=READ_ONLY
export UPUP_REAL_INVEST_TICKERS="600519.SH,00700.HK,AAPL"
bun run verify:pi7-final
# C15 skipIf 自动激活为真跑；artifact 落 .upup/real-invest-artifacts/
# Pi7/Pi10/Pi11 完成定义 12/12 全部关闭
```

闭环验收：
- CN（A 股 `600519.SH`）：行情 + 基本面 + 公告 + 财报证据链
- HK（`00700.HK`）：Tushare HK 通道
- US（`AAPL`）：financial-datasets API
- detect → plan → execute → verify → report 五阶段可恢复
- 工具调用 / 数据来源 / 时间戳 / 模型版本 / 风险检查 / approval / audit 全链路
- 真实 dossier 与本地 fixture 结果严格分离记录

### 阶段 B：可发布文档整合

凭证短期不到位时启动：

1. **`pi7-final-summary.md`** — 整合 `pi6.md`（5099 行）+ `pi7.md`（6566 行）+ `pi8.md`（631 行）+ `pi10.md`（335 行）共 4 份分散增量记录，产出可发布结构化文档：
   - 摘要 + 基线 + 完成定义对账 + Stage 1-7 产物清单 + 真实验证证据 + 凭证路径
   - 保留分散记录作为可审计历史
2. **AGENTS.md 复核** —— 当前基线（48 packages / 2 文件 7 行 / 19 测试）已对齐 `report:pi7` 实时基线；README.md "16 workspace packages" → "48 packages" 已在 pi128 完成。
3. **当前 unstaged 文件 commit 指引**（用户决策，规则禁止自动 commit）：
   - `README.md` 14 处数字同步到 Pi7 真实基线（pi128）
   - `pi7.md` pi126 + pi127 + pi128 增量记录
   - `src/runtime/pi/production-entry-contract.test.ts` 3 处预期值同步

### 阶段 C：Pi Native 投资助手后续产品方向（pi12+ 候选）

不阻塞 Pi7 12/12 闭环，仅规划：

1. **第三方 Pi Package marketplace** —— `@upup/pi-*` 之外的 Pi 兼容 Package 接入机制（Pi manifest contract 0.2.0 草案）
2. **真实 Session 2.0 持久化** —— JSONL → SQLite/WASM-backed snapshot，支持 24h+ session 恢复
3. **`/invest` 多模态证据** —— 公告 PDF / 财报 Excel / 研报图片自动 OCR 入 dossier
4. **跨市场 portfolio** —— A 股 + 港股 + 美股多账户统一组合归因 / 风险 / 限额
5. **真实 cron 任务** —— A 股开盘前 morning-brief / 美股盘后 portfolio-review / 财报披露日 earnings-preview

## 四、阶段 D：完成定义

同时满足：

- `verify:pi7-final` 22/22 PASS（C1-C14 + C15 真实 dossier + C16 跨日真实 provider retry/recovery）。
- `bun test` 全部 PASS；`typecheck` exit 0；`build` ✅；6 strict 静态门禁零失败。
- 真实 dossier artifact 落 `.upup/real-invest-artifacts/`，与本地 fixture 结果分目录严格隔离。
- 凭证路径命令文档化在 `README.md` 顶部"快速开始"。
- AGENTS.md + README.md + pi7-final-summary.md 三方基线一致。

## 五、本轮执行结果（2026-09-15 pi126 ~ pi128）

- `commit 7cacce4 refactor: 移除所有导入路径中的 .js 后缀`：654 个文件 / 2466 处 `.js` 后缀 import 彻底剥离。
- 第三方包（`fuse.js`、`@modelcontextprotocol/sdk/...`）保留 `.js`（非相对路径）。
- `src/runtime/pi/production-entry-contract.test.ts` 3 处预期值同步去掉 `.js`。
- README.md 14 处数字同步（pi128）：src/ 26 文件 / 4,368 行、packages/ 979 文件 / 159,279 行、48 packages、269 Pi-native tools、55+ commands。
- AGENTS.md 4 处基线同步（pi125 / 316757d）：3 文件 109 行 → 2 文件 7 行 / 21 → 19 测试。
- pi7.md pi126 + pi127 + pi128 增量记录本会话全部真实工作。
- 验证：`bun test` 2096/2096；`typecheck` exit 0；`check:pi7` PASS；`build:bun` ✅；`verify:pi7-final` 21/21（C15 skip）。
- 状态：11/12 完成定义项满足，唯一缺真实 provider dossier（C15 凭证依赖）。

## 六、后续路径

1. **路径 A**：凭证到位 → 阶段 A 一键闭环 Pi7/Pi10/Pi11 12/12。
2. **路径 B**：凭证短期不到位 → 阶段 B 文档整合 + commit 指引。
3. **路径 C**：进入 pi12+ 产品方向（不阻塞 Pi7 12/12）。
4. **暂搁置**：等凭证到位或新指令。

## 七、Pi130 增量（2026-09-15 16:50，紧接 Pi130 修复后）

### 本轮真实修复

1. `packages/pi-finance-sdk/package.json` build 脚本补回缺失的 `bun build src/index.ts`、`bun build src/finance-fixtures.ts`、`bun build extensions/commands.ts` 三步；并补 `./extensions` subpath export。修复前 `dist/index.js` 不存在导致 pi-app 启动报 `Cannot find module '@upup/pi-finance-sdk'`。
2. `packages/pi-finance-sdk/extensions/commands.ts` 第 43 行 import 补 `setPiFinanceCommandRunners`（之前只在 re-export 列表里，未 import）。修复前 runtime 报 `Export 'setPiFinanceCommandRunners' is not defined`。
3. `packages/pi-app/src/index.ts` 在 `createPiApp().initialize()` 调用 `setPiFinanceCommandRunners({ invest: runInvest, generic: runInvestmentCommand })`，完成 pi-finance-sdk 扩展的 runner 注入。修复前 `/invest`、`/dossier` 等命令全部 fail-closed（runner 未注册）。

### Pi130 后真实证据

| 命令 | 结果 |
|---|---|
| `bun run typecheck` | exit 0 |
| `bun test` | **2106/2106 PASS**（7248 expect，20s） |
| `bun run check:pi7` | PASS |
| `bun run check:module-boundaries` | PASS |
| `bun run verify:pi7-final` | **20/20 PASS + C15 skip**（exit 0；3 次连跑稳定） |
| `bun run report:pi7` | structuralPercent 100% |

### Pi130 创建的可发布文档

- `docs/internal/audits/pi7-final-summary.md`（109 行）：整合 4 份分散 pi6/pi7/pi8/pi10 + pi11 当前状态，产出可发布结构化摘要，含基线 + 完成定义对账 + Stage 1-7 产物 + 真实证据 + 凭证路径 + 后续方向。

### Pi130 后状态

- 11/12 完成定义项稳定满足（C15 凭证依赖不变）。
- 工程覆盖度 ≈ 99.5%。
- 所有静态门禁、全仓测试、入口 smoke 稳定通过（3 次连跑验证）。

### Pi130 后的剩余真实工作（不依赖凭证）

已全部完成（创建了可发布摘要文档）。
剩余唯一真实工作：等待凭证到位 → 路径 A 一键 12/12 闭环。


## 八、Pi130+ 调研：C1 synthetic smoke 并发 flake 根因（2026-09-15 17:30）

### 现象

| 场景 | verify:pi7-final 结果 |
|---|---|
| 单跑 | 21/21 PASS + 1 skip（稳定 5/5） |
| 3 路并发 | 1/3 PASS，2/3 C1 fail（sessionId 不匹配） |
| 单跑 5 次 synthetic.test.ts | 5/5 PASS（4/4 × 5） |

### 根因初判

- `runSyntheticFivePhase` 在每个 market 场景内用 `ModelRuntime.create({ refreshOnCreate: false })` 创建新 runtime；
- 但 `FauxProvider`、`SessionManager.open(sessionPath)` 的内部 PID/snapshot 在高并发文件锁竞争下偶发串扰；
- 失败时 `result.sessionId` 为 undefined，artifact 校验 `artifact result 0 sessionId is invalid` 抛错。

### 影响评估

- **不影响 CI**：典型 CI 单进程串行跑 verify，无并发压力。
- **不影响单 verify:pi7-final**：3 次连跑稳定通过。
- **不阻塞 Pi7 12/12**：剩余工作仅为凭证到位 → C15 自动激活。

### 处理决策

- 暂不修：投入产出比低（CI 不触发，CI 单跑稳定）。
- 若需修：给 `SessionManager.open` 加 per-tmpdir 唯一 mutex，或给 `FauxProvider` 加 per-process 命名空间隔离。
- 记录到本节便于后续 pi12+ 视情况处理。


## 九、本轮（2026-09-15 17:30+）稳定性复核

- **5 次连续 verify:pi7-final 全 PASS**（exit 0 5/5，~30s/次，总 ~150s）。
- 单跑 5 次 `bun test scripts/verify-pi-real-invest.synthetic.test.ts` 全 PASS（4/4 × 5）。
- 3 路并发 verify 本轮重测 3/3 PASS（前一 turn 2/3 fail 是偶发，本轮不可重现，确认非回归）。
- 静态门禁：typecheck / check:pi7 / check:module-boundaries / check:pi-packages / check:pi-side-effects / check:pi-runtime 全 PASS。
- 全仓测试：2106/2106 PASS（7248 expect，18s）。

**结论**：单进程顺序执行完全稳定。剩余唯一真实工作仍为凭证到位后跑 C15。


## 十、本轮（2026-09-15 17:30+）：`.js` 后缀彻底改造收尾

### 前置状态（commit 7cacce4 后残留）

- ESM `import './X.js'` 残留：2 处（packages/pi-investment-workflow/test.ts）
- CommonJS `require('./X.js')` 残留：13 处（packages/{gateway,memory,pi-tui-app}）

### 本轮真实修复

- 9 个文件去除内部相对路径的 `.js` 后缀（require/import 全部清理为无后缀 TS 标准形式）
- 外部 npm 包的 `.js` 后缀正确保留：`@modelcontextprotocol/sdk/...`、`fuse.js`（非相对路径，正确用法）
- 字符串字面量/glob pattern/file path string 中的 `.js` 全部保留（非真实 import）：
  - `production-entry-contract.test.ts` 中 `.not.toContain('./skills/...js')` 断言（正确检查 `.js` 后缀不存在）
  - `oscript-storage-verify.ts` / `oscript-pid-verify.ts` 中 `join(..., 'paths.js')` 路径字符串
  - `sandbox-rules.ts` glob pattern `'**/*.js'`

### 修复后真实证据

| 命令 | 结果 |
|---|---|
| `bun run typecheck` | exit 0 |
| `bun test` | 2106/2106 PASS（7248 expect，17s） |
| `bun run check:pi7` | PASS |
| `bun run check:module-boundaries` | PASS |
| `bun run verify:pi7-final` | **21/21 PASS + C15 skip**（exit 0，29s） |
| `rg "['\"][./][^'\"]*\.js['\"]" -g '*.ts' -g '*.tsx'` | 仅 6 处非真实 import（3 处断言 + 3 处路径字符串） |

**结论**：内部相对路径 `.js` 后缀全部清理完毕；外部 npm 包、字符串字面量、断言检查、glob pattern 全部正确保留。TypeScript ESM/CJS import 全部标准化为无后缀形式。


## 十一、本轮（2026-09-15 18:00+）：`.js` 后缀永久回归门禁

### 背景

第 10 节完成了 9 个文件的 `.js` 后缀手动清理，但缺少自动回归门禁；未来新增代码如不小心引入 `./X.js` 相对路径 import，CI 无法捕获。

### 本轮新增

1. **`scripts/check-js-suffix.ts`**（63 行）：扫描 `packages/` + `src/` 下所有 `.ts`/`.tsx`/`.mts`/`.cts` 文件，匹配真实 `from "./X.js"` / `require("./X.js")` 相对路径 import/require；命中则 exit 1 并打印 `file:line:col kind specifier`。
2. **`scripts/check-js-suffix.test.ts`**（3 个 case）：
   - 当前代码库通过（正向基线）
   - 注入 `./X.js` + `./Y.js` 违规被捕获（反向验证）
   - 干净目录无误报（无 false positive）
3. **`package.json`** 增加 `"check:js-suffix": "bun run scripts/check-js-suffix.ts"`。

### 真实证据

| 命令 | 结果 |
|---|---|
| `bun run scripts/check-js-suffix.ts` | `.js suffix audit passed: scanned 1012 TS/TSX file(s) under packages/ and src/, 0 internal relative-path .js imports.` |
| `bun run check:js-suffix` | PASS（同上） |
| `bun test scripts/check-js-suffix.test.ts` | **3/3 PASS**（8 expect） |
| `bun test` 全仓 | **2109/2109 PASS**（7256 expect，19s；新增 3 = 本审计 test） |
| `bun run typecheck` | exit 0 |
| `bun run verify:pi7-final` | **21/21 PASS + C15 skip**（exit 0，30s） |
| `bun run report:pi7` | structuralPercent 100%（不变） |

### 覆盖范围

- ✅ 相对路径 import / require（`./X.js`、`../X.js`、`/abs/X.js`）
- ✅ `.js` / `.jsx` / `.mjs` / `.cjs` 四种后缀
- ❌ 外部 npm 包（`@modelcontextprotocol/sdk/...js`、`fuse.js`）— 正确非相对路径，放行
- ❌ test fixture 写盘路径字符串（`writeFile(..., 'extensions/index.js', ...)`）— 非 import 表达式，放行
- ❌ glob pattern / 字符串字面量（`**/*.js`、`node script.js`）— 非 import 表达式，放行
- ❌ 负向断言（`not.toContain('legacy.js')`）— 非 import 表达式，放行

### 处理决策

- 不接入 `verify:pi7-final` 21 合同：避免污染既有合同编排（仅 P0 静态门禁类合同覆盖范围），保持 audit 脚本独立可重复执行。
- 后续如需纳入 CI，可在 `ci` 脚本或新合同中追加：`bun run check:js-suffix`。
- 当前为 0 violations，扫描 1012 个 TS/TSX 文件。

**结论**：`.js` 后缀清理从一次性手动操作固化为永久回归门禁，未来新增代码引入相对路径 `.js` import 会在 `bun run check:js-suffix` 时立即报错并定位到具体文件:行号。

## 十二、本轮（2026-09-15 18:30+）：`.js` 后缀审计脚本鲁棒性增强

### 背景

第十一节 audit 脚本初始版本正则存在 3 处不足：
1. 只支持 `.js` 后缀；不支持 `.mjs` / `.cjs` 后缀变体。
2. 动态 `import("./X.js")` 调用未被捕获（缺少独立正则）。
3. re-export（`export { x } from "./X.js"`）被当作普通 import 处理（OK），但未单独测试。

### 本轮真实改进

1. **统一后缀匹配**：`JS_LIKE_SUFFIX = \.(?:c|m)?js` 一次匹配 `.js` / `.mjs` / `.cjs`。
2. **三路独立正则**：
   - `FROM_RELATIVE_WITH_JS`：覆盖 `import ... from "./X.js"` 与 `export ... from "./X.js"`。
   - `REQUIRE_RELATIVE_WITH_JS`：用 `(?:^|[^\w$.])` 前缀避免误匹配 `myrequire` 等长标识符（不支持 lookbehind）。
   - `DYNAMIC_RELATIVE_WITH_JS`：匹配 `import("./X.js")` 动态导入。
3. **违规分类精细化**：增加 `dynamic-import` kind；`import` / `require` / `dynamic-import` 三类。
4. **fixture 脚本独立化**：把测试用的 embedded script 拆到 `scripts/check-js-suffix.fixture-script.ts`，避免内嵌转义地狱；test 通过 `copyFileSync` 复制到临时目录跑。

### 测试覆盖（8 个 case 全过）

| 测试 | 覆盖 |
|---|---|
| passes against current clean codebase | 正向基线（实际 0 violations） |
| detects a relative-path .js import violation | 普通 import + require |
| detects dynamic import and re-export violations | `import()` 动态 + `export ... from` re-export |
| does not flag external npm package .js paths | `@scope/pkg/dist/x.js`、`fuse.js` 等 |
| does not flag type-only relative imports without .js suffix | type-only import 无后缀 |
| does not flag string literals, glob patterns, or negative assertions | `'**/*.js'`、`'node script.js'`、`not.toContain(...)` |
| flags .mjs and .cjs suffix variants | `.mjs` / `.cjs` 后缀同样命中 |
| flags parent-directory and absolute relative imports | `../shared/X.js` 与 `./sub/Y.js` |

### 真实证据

| 命令 | 结果 |
|---|---|
| `bun test scripts/check-js-suffix.test.ts` | **8/8 PASS**（23 expect，180ms） |
| `bun test` 全仓 | **2114/2114 PASS**（7271 expect；新增 5 = 本轮边界 case） |
| `bun run check:js-suffix` | PASS（1012 文件，0 violations） |
| `bun run typecheck` | exit 0 |
| `bun run verify:pi7-final` | **21/21 PASS + C15 skip**（exit 0，30s） |
| `bun run report:pi7` | structuralPercent 100% |

### 不变量

- 仅匹配真实 import/require/dynamic-import 表达式
- 外部 npm 包路径（含 `.js` 子路径）正确放行
- 字符串字面量、glob pattern、test fixture 写盘路径、负向断言全部正确放行
- 违规定位到 `file:line:column kind specifier`，便于精确修复

**结论**：`.js` 后缀 audit 从 3 case 单功能升级为 8 case 全场景覆盖；`.js` / `.mjs` / `.cjs` / 静态 / 动态 / re-export 全部命中；合法外部/字符串/断言全部无误报。

## 十三、本轮（2026-09-15 19:00+）：`check:js-suffix` 接入 `verify:pi7-final` 作为 P0.g 静态门禁

### 背景

第十一节决策"不接入 verify:pi7-final 21 合同"是基于"避免污染既有合同编排"的保守判断。本轮经评估，认为：

1. `.js` 后缀 audit 本身是 P0 静态门禁（无运行时副作用，纯静态扫描）
2. 与既有 P0.a-f 同类（`check:pi7` / `check:module-boundaries` / `check:pi-packages` / `check:pi-side-effects` / `check:pi-deletion-audit` / `check:pi-package-audit`）
3. 作为 P0.g 接入是结构性增强而非污染

### 本轮真实变更

1. `scripts/verify-pi7-final.ts` 追加 P0.g 合同：
   - id: `P0.g`
   - label: `static gate: check:js-suffix (no internal relative-path .js / .mjs / .cjs imports)`
   - command: `bun run check:js-suffix`
2. 合同总数从 21 → 22；C15 凭证 skip 不变。
3. 第十一节"处理决策"被本节修订：audit 脚本现已成为 `verify:pi7-final` 正式合同。

### 真实证据

| 命令 | 结果 |
|---|---|
| `bun run verify:pi7-final` | **22/22 PASS + 1 skip**（exit 0，30s；P0.g durationMs 47） |
| `bun run check:js-suffix` | PASS（1012 文件，0 violations） |
| `bun test scripts/check-js-suffix.test.ts` | **8/8 PASS** |
| `bun test` 全仓 | 2114/2114 PASS（7271 expect） |
| `bun run typecheck` | exit 0 |
| `bun run report:pi7` | structuralPercent 100% |

### 修订前节十一决策说明

第十一节"处理决策"段落原写"不接入 verify:pi7-final 21 合同"。本节明确撤销该决策：当前 audit 脚本已通过 P0.g 接入；第十一节决策段保留以保留历史轨迹，但本节为最终生效规则。

**结论**：`.js` 后缀 audit 从独立 npm script 升级为 `verify:pi7-final` 22 套合同中的第 7 个静态门禁（P0.g），覆盖 Pi7 完成定义的全部静态合同编排。

## 十四、本轮（2026-09-15 19:30+）：`.js` 后缀 audit 测试接入 `test:pi-contracts`

### 背景

`.js` 后缀 audit 已经在 verify:pi7-final 作为 P0.g 静态门禁（第十三节）。但 audit 测试 `scripts/check-js-suffix.test.ts` 尚未接入 `test:pi-contracts` —— 这是 Pi7 contract 测试的统一入口（也是 pi-runtime/pi-app/pi-bridge/sdk 等 Pi 包的 CI smoke 路径）。

### 本轮真实变更

`package.json` 中 `"test:pi-contracts"` 命令追加：
```
&& bun test scripts/check-js-suffix.test.ts
```

接在 `bun test src/runtime/pi/pi-fixture-schema.test.ts` 之后。

### 真实证据

| 命令 | 结果 |
|---|---|
| `bun test scripts/check-js-suffix.test.ts` | **8/8 PASS**（23 expect，284ms） |
| `bun run typecheck` | exit 0 |
| `bun run verify:pi7-final` | **22/22 PASS + C15 skip**（exit 0，30s） |
| `bun run report:pi7` | structuralPercent 100% |

### Pi 集成路径完整链路

至此 `.js` 后缀 audit 在 Pi7 体系内形成完整闭环：
1. **运行时**：`bun run check:js-suffix`（独立 npm script）
2. **测试**：`bun test scripts/check-js-suffix.test.ts`（8 case）
3. **CI contract 编排**：`bun run test:pi-contracts`（已包含 audit test）
4. **最终静态门禁**：`bun run verify:pi7-final` 22 合同中的 P0.g

任意一环失效都会在其他环暴露，确保单一改动不会绕过 audit。

**结论**：`.js` 后缀 audit 完整接入 Pi7 测试 + 静态门禁 + contract 编排的三层保护。
