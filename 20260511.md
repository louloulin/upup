# 20260511.md — Claude Agent SDK 对标改造计划

> 创建日期: 2026-05-11 | 版本: 1.0 | 目标: 对标 Claude Code Agent SDK 实现

---

## 1. 执行摘要

本计划旨在将 UpUp AI Agent 改造为类似 Claude Code 的 Agent SDK 架构。核心目标是:

1. **提取 @upup/agent-core**: 独立的核心 Agent 运行时包
2. **标准化工具接口**: 与 Claude Agent SDK 对齐
3. **增强子代理系统**: 支持多 Agent 并行和层级协作
4. **完善 MCP 集成**: 支持外部工具和服务发现
5. **流式输出标准化**: 完整的流式响应支持

---

## 2. 当前架构分析

### 2.1 现有组件

```
src/agent/
├── agent.ts           # 主 Agent 类 (核心)
├── subagent.ts        # 子代理接口
├── subagent-runner.ts # 子代理执行器
├── subagent/
│   └── types.ts      # EnhancedSubagentRunner + 内置 Agent
├── tool-executor.ts   # 工具执行
├── compaction.ts     # 上下文压缩
├── prompts.ts        # 提示词模板
├── types.ts          # 类型定义
└── index.ts          # 导出

src/tools/
├── registry/         # 工具注册表
├── bash/            # Bash 工具
├── filesystem/      # 文件系统工具
├── quant/           # 量化金融工具
├── research/        # 研究工具
└── ...              # 30+ 工具

src/mcp/
├── client.ts        # MCP 客户端
└── adapters/        # MCP 适配器
```

### 2.2 与 Claude Agent SDK 对比

| 特性 | UpUp 当前 | Claude Agent SDK | 差距 |
|------|----------|------------------|------|
| Agent 运行时 | 单一 Agent 类 | Agent 类 + Runner | 🔴 需要重构 |
| 工具定义 | 自定义 DynamicStructuredTool | 标准化 Tool 接口 | 🟡 部分对齐 |
| 子代理 | EnhancedSubagentRunner | 多 Agent 并行 | 🟡 需标准化 |
| MCP 集成 | 基础支持 | 完整 MCP 协议 | 🟡 需增强 |
| 流式输出 | 部分支持 | 完整 Server-Sent Events | 🔴 需完善 |
| 状态管理 | 分散 | Centralized state | 🔴 需重构 |
| 上下文管理 | 手动压缩 | 自动上下文管理 | 🟡 部分实现 |

---

## 3. Claude Agent SDK 核心功能分析

### 3.1 Agent 类 (参考)

```typescript
// Claude Agent SDK 核心接口
interface AgentConfig {
  model: string;
  systemPrompt?: string;
  tools: Tool[];
  maxTokens?: number;
  temperature?: number;
}

class Agent {
  constructor(config: AgentConfig);
  run(input: string): Promise<string>;
  stream(input: string): AsyncGenerator<string>;
  stop(): void;
}
```

### 3.2 关键功能点

1. **标准工具接口**: `Tool` 接口规范化
2. **流式响应**: 支持 `AsyncGenerator` 流式输出
3. **多轮对话**: 内置消息历史管理
4. **上下文窗口**: 自动上下文压缩
5. **工具调用**: 标准化的 `useTool` 函数
6. **错误恢复**: 内置重试和回退机制

---

## 4. 改造计划

### Phase A: 核心抽象 (A1-A3)

#### A1: 提取 @upup/agent-core 包

**目标**: 创建独立的 Agent 运行时核心

**新建文件**:
```
packages/agent-core/src/
├── index.ts           # 导出
├── agent.ts          # Agent 类
├── runner.ts         # AgentRunner
├── message.ts        # 消息类型
├── tool.ts           # 标准工具接口
├── config.ts         # AgentConfig
└── types.ts          # 共享类型
```

**关键接口**:
```typescript
// agent-core/src/tool.ts
export interface Tool<Name extends string = string> {
  name: Name;
  description: string;
  inputSchema: z.ZodType;
  execute(input: unknown): Promise<string>;
}

// agent-core/src/agent.ts
export class Agent {
  constructor(config: AgentConfig);
  run(input: string, options?: RunOptions): Promise<AgentResult>;
  stream(input: string): AsyncGenerator<StreamEvent>;
  stop(): void;
}
```

#### A2: 标准化工具体系

**目标**: 将现有工具迁移到标准 Tool 接口

**迁移内容**:
- `src/tools/registry/types.ts` → 标准化工具定义
- `src/tools/bash/bash-tool.ts` → 实现标准 Tool 接口
- `src/tools/filesystem/` → 实现标准 Tool 接口

#### A3: 流式输出标准化

**目标**: 完善 SSE 流式响应

**新建文件**:
```
packages/agent-core/src/
├── stream.ts         # 流式输出
├── events.ts         # 事件类型
└── sse.ts           # SSE 工具函数
```

---

### Phase B: 子代理系统 (B1-B2)

#### B1: 标准化 Agent 创建

**目标**: 支持内置 Agent 和自定义 Agent

**新建文件**:
```
packages/agent-core/src/
├── builtins/        # 内置 Agent
│   ├── index.ts
│   ├── debugger.ts
│   ├── reviewer.ts
│   ├── researcher.ts
│   └── tester.ts
└── agent-factory.ts  # Agent 工厂
```

#### B2: 多 Agent 并行

**目标**: 支持 Agent 层级协作

**实现**:
```typescript
// 多 Agent 并行执行
const results = await Promise.all([
  agent1.run(task1),
  agent2.run(task2),
  agent3.run(task3),
]);

// Agent 协作
const result = await agent1.run(task1, {
  subagents: [agent2, agent3],
});
```

---

### Phase C: MCP 集成 (C1-C2)

#### C1: MCP 协议完整实现

**目标**: 完整支持 MCP 协议

**增强内容**:
- 服务发现 (`tools/list`, `resources/list`)
- 采样请求 (`sampling/createMessage`)
- 进度通知

#### C2: MCP 工具桥接

**目标**: MCP 工具与原生工具统一

```typescript
// MCP 工具转换为标准 Tool
function mcpToolToStandard(mcpTool: MCPTool): Tool {
  return {
    name: mcpTool.name,
    description: mcpTool.description,
    inputSchema: z.object(mcpTool.inputSchema),
    execute: async (input) => {
      const result = await mcpClient.callTool(mcpTool.name, input);
      return formatResult(result);
    },
  };
}
```

---

### Phase D: 工具注册表重构 (D1-D2)

#### D1: 标准工具注册表

**目标**: 统一的工具发现和管理

```typescript
// packages/agent-core/src/registry.ts
export class ToolRegistry {
  register(tool: Tool): void;
  unregister(name: string): boolean;
  get(name: string): Tool | undefined;
  list(): Tool[];
  search(query: string): Tool[];
}
```

#### D2: 工具分类和标签

**目标**: 更好的工具组织

```typescript
interface ToolMetadata {
  name: string;
  category: 'filesystem' | 'code' | 'research' | 'finance' | 'system';
  tags: string[];
  author?: string;
  version?: string;
}
```

---

## 5. 文件变更总结

### 5.1 新建文件

| 文件 | 行数 | 优先级 |
|------|------|--------|
| packages/agent-core/src/agent.ts | ~200 | P0 |
| packages/agent-core/src/tool.ts | ~100 | P0 |
| packages/agent-core/src/runner.ts | ~150 | P0 |
| packages/agent-core/src/stream.ts | ~100 | P1 |
| packages/agent-core/src/builtins/*.ts | ~300 | P1 |
| packages/agent-core/src/registry.ts | ~100 | P0 |

### 5.2 修改文件

| 文件 | 变更 | 优先级 |
|------|------|--------|
| src/agent/agent.ts | 重构为使用 agent-core | P0 |
| src/tools/registry/types.ts | 标准化工具定义 | P0 |
| src/tools/bash/bash-tool.ts | 实现标准 Tool 接口 | P1 |
| src/mcp/client.ts | 增强 MCP 支持 | P2 |

### 5.3 删除/弃用

| 文件 | 说明 |
|------|------|
| src/agent/subagent.ts | 迁移到 agent-core |

---

## 6. 实施顺序

```
Week 1: Phase A1 - Agent Core 提取
Week 2: Phase A2 - 工具标准化 + A3 流式输出
Week 3: Phase B1 - 内置 Agent
Week 4: Phase B2 - 多 Agent 并行
Week 5: Phase C - MCP 集成
Week 6: Phase D - 工具注册表
```

---

## 7. 验收标准

- [ ] `@upup/agent-core` 包可独立使用
- [ ] 所有内置工具实现标准 Tool 接口
- [ ] 流式输出完整支持 SSE
- [ ] MCP 工具可无缝转换为标准 Tool
- [ ] 多 Agent 并行执行正常
- [ ] 现有测试通过率 > 95%

---

## 8. 风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 重构破坏现有功能 | 高 | 分阶段实施, 保持向后兼容 |
| 工具接口变更 | 中 | 提供适配器层 |
| 测试覆盖不足 | 中 | 优先补充测试 |

---

## 9. 参考资料

- [Claude Agent SDK 文档](https://docs.anthropic.com/claude-code)
- [LangChain Agent 架构](https://python.langchain.com/docs/modules/agents/)
- [MCP 协议规范](https://modelcontextprotocol.io/)

---

*最后更新: 2026-05-11*