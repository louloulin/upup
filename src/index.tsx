#!/usr/bin/env bun
/**
 * src/index.tsx - Application entry shell.
 *
 * This file is intentionally minimal. All business logic lives in workspace
 * packages under packages/. See `@upup/index-app` for the full entry shell.
 */
import { run } from '@upup/index-app';
run();
