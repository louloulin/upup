/**
 * @deprecated Use @upup/utils instead
 * Re-exports from @upup/utils for backward compatibility
 */
export { getPlatform, isMacOS, isWindows, isLinux, isWSL, SUPPORTED_PLATFORMS, type Platform } from '@upup/utils';
