/**
 * @deprecated Moved to @upup/pi-storage. This facade will be removed.
 *
 * Round 6.3: src/storage/* has been physically migrated into the
 * `@upup/pi-storage` workspace package. This thin facade keeps
 * existing root-side consumers working until they are updated.
 */
export * from '@upup/pi-storage';
