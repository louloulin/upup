/**
 * src/theme.ts - Application entry shell for the theme constants.
 *
 * This file is intentionally minimal. The actual theme definitions
 * live in `@upup/tui-renderer/theme`.
 */
export {
  theme,
  markdownTheme,
  selectListTheme,
  editorTheme,
} from '@upup/tui-renderer/theme';
