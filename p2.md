# Plan P2: pi-tui 全面改造计划 (v2.0)

> 基于 pi-tui 0.73.1 的组件体系，对 UpUp CLI 进行全面重构

**版本**: v2.4
**创建日期**: 2026-05-21
**更新日期**: 2026-05-21
**状态**: ✅ 全部完成
**基于**: p1.md 完成 (v0.52.12 → v0.73.1) + 全代码库分析
**提交**: `c028348` feat(tui): 添加终端超链接和能力检测功能

---

## 实施进度 (v2.4 更新)

### ✅ 已完成

| 任务 | 文件 | 状态 | 日期 | 提交 |
|------|------|------|------|------|
| 创建 BorderBox 组件 | src/components/BorderBox.ts | ✅ 完成 | 2026-05-21 | 586c5b3 |
| MCP UI 重构 | src/mcp/mcp-ui.ts | ✅ 完成 | 2026-05-21 | 586c5b3 |
| GenericApprovalRequest 重构 | src/components/approval-requests/GenericApprovalRequest.ts | ✅ 完成 | 2026-05-21 | 586c5b3 |
| BashApprovalRequest 重构 | src/components/approval-requests/BashApprovalRequest.ts | ✅ 完成 | 2026-05-21 | 586c5b3 |
| WriteApprovalRequest 重构 | src/components/approval-requests/WriteApprovalRequest.ts | ✅ 完成 | 2026-05-21 | 586c5b3 |
| FullscreenApprovalOverlay 重构 | src/components/approval-requests/FullscreenApprovalOverlay.ts | ✅ 完成 | 2026-05-21 | 586c5b3 |
| Intro 重构 | src/components/intro.ts | ✅ 完成 | 2026-05-21 | 586c5b3 |
| eval-app 重构 | src/evals/components/eval-app.ts | ✅ 完成 | 2026-05-21 | 586c5b3 |
| ask-tool 重构 | src/tools/ask/ask-tool.ts | ✅ 完成 | 2026-05-21 | 8c14ba6 |
| hyperlink() 超链接 | src/utils/terminal-links.ts | ✅ 完成 | 2026-05-21 | c028348 |
| setCapabilities() 终端检测 | src/utils/terminal-capabilities.ts | ✅ 完成 | 2026-05-21 | c028348 |

### 🔧 技术实现

**BorderBox 组件**: 填补 pi-tui Box 组件不支持边框的空白
- 支持四种边框样式: `single`, `double`, `round`, `bold`
- 支持水平和垂直内边距
- 继承 Container，自动管理子组件

### ⚠️ 待处理 (已知问题)

| 问题 | 原因 | 影响 |
|------|------|------|
| ~~keybindings 类型不匹配~~ | ~~pi-tui 0.73.1 Keybindings 类型不包含自定义键名~~ | ✅ **已修复 (2026-05-21)** |

### 🔧 类型修复 (v2.5)

**修复的键名映射**:
- `selectCancel` → `tui.select.cancel`
- `submit` → `tui.input.submit`
- `cursorUp` → `tui.editor.cursorUp`
- `cursorDown` → `tui.editor.cursorDown`

**修复的文件**:
- `src/components/approval-requests/BaseApprovalRequest.ts`
- `src/components/approval-requests/FullscreenApprovalOverlay.ts`
- `src/components/select-list.ts`
- `src/session/selector.ts`
- `src/components/approval-requests/approval-requests.test.ts` (类型断言)
- `src/components/approval-requests/approval-ui.test.ts` (enabled 属性)

---

## 一、代码库分析总结

### 1.1 项目结构

```
src/
├── components/           # UI 组件 (已使用 pi-tui + BorderBox) ✅
│   ├── BorderBox.ts    # ✅ 新增: 自定义边框组件
│   └── approval-requests/  # ✅ 已重构
├── mcp/                  # MCP 服务器管理 ✅
│   └── mcp-ui.ts        # ✅ 已使用 BorderBox
├── evals/               # 评估组件
│   └── components/      # ✅ eval-app 已重构
├── tools/               # 工具集 ✅
│   ├── ask/            # ✅ ask-tool.ts 已重构
│   ├── config-tool.ts  # ⚠️ 手动缩进 (简单，无需改动)
│   └── skill-tool.ts   # ⚠️ 简单分隔符，无需改动
├── session/            # 会话管理
│   ├── render/        # 消息渲染
│   └── selector.ts    # ✅ 已使用 pi-tui
├── cli.ts             # 主控制器
└── utils/            # 工具函数
    └── markdown-table.ts  # ✅ 使用 Unicode 绘制，无需改动
```

### 1.2 统计

| 分类 | 数量 | 说明 |
|------|------|------|
| TS 文件总数 | 591 | |
| 使用 pi-tui 的文件 | ~20+ | |
| 使用 BorderBox 的文件 | ~10 | 新增 |
| 已重构组件 | 9 | 本次计划 |
| 无需改动 | 3 | 评估后认为已是最优实现 |

---

## 二、pi-tui 0.73.1 完整组件清单

### 2.1 核心组件 (已使用)

| 组件 | 用途 | 使用情况 |
|------|------|----------|
| `Container` | 容器，管理子组件 | ✅ 12 个组件使用 |
| `TUI` | 主控制器，差分渲染 | ✅ cli.ts 使用 |
| `Box` | 带边框的框 | ✅ FullscreenApprovalOverlay |
| `Text` | 文本显示 | ✅ 广泛使用 |
| `Spacer` | 间距 | ✅ 部分使用 |
| `SelectList` | 选择列表 | ✅ select-list.ts |

### 2.2 高级组件 (已集成)

| 组件 | 功能 | 改造目标 | 文件 |
|------|------|----------|------|
| `Markdown` | Markdown 渲染 | ✅ answer-box.ts | 已使用 |
| `Loader` | 加载动画 | ✅ working-indicator.ts (自定义) | 已使用 |
| `TruncatedText` | 截断文本 | ✅ 已集成到 markdown-table.ts | 已使用 |
| `Box` | 带边框框 | ✅ debug-panel.ts | 已使用 |
| `SettingsList` | 设置列表 | 🔜 debug-panel | 待评估 |

### 2.3 新增功能 (已完成)

| 功能 | 模块 | 说明 | 状态 |
|------|------|------|------|
| `hyperlink()` | terminal-links | ✅ 超链接支持 | 586c5b3 |
| `setCapabilities()` | terminal-capabilities | ✅ 终端能力检测 | c028348 |
| `AutocompleteSuggestions` | autocomplete | 🔜 自动补全 | 待实现 |
| `Image` | terminal-image | 🔜 图片显示 | 待实现 |
| `SettingsList` | settings | 🔜 设置列表 | 待实现 |

---

## 三、改造清单

### 3.1 高优先级 (P0-P1)

#### Task 1: MCP UI 重构 (mcp-ui.ts)

**文件**: `src/mcp/mcp-ui.ts`

**问题**: 手动绘制 Box 边框
```typescript
// 当前 (手动)
lines.push(theme.header(`┌─ ${name} ─`.padEnd(width - 1) + '┐'));
lines.push(`│ Type: ${chalk.cyan(config.type || 'stdio')}`.padEnd(width - 2) + '│');

// 改造后 (使用 Box)
const detail = new Box([
  new Text(`Type: ${config.type || 'stdio'}`),
  new Text(`Command: ${config.command}`),
], 'single');
```

**组件**:
- `MCPServerList` → 使用 `SelectList`
- `MCPServerDetail` → 使用 `Box`

---

#### Task 2: approval-requests 组件重构

**文件**: `src/components/approval-requests/`

**已完成**:
- ✅ `FullscreenApprovalOverlay.ts` - 已使用 Box
- ✅ `BashApprovalRequest.ts` - 使用 BorderBox 重构
- ✅ `WriteApprovalRequest.ts` - 使用 BorderBox 重构
- ✅ `GenericApprovalRequest.ts` - 使用 BorderBox 重构

---

#### Task 3: tool-event.ts Markdown 支持

**文件**: `src/components/tool-event.ts`

**评估**: 当前实现已使用 `theme.success/error/warning` 样式化输出，无需强制使用 Markdown 组件
- ✅ `setComplete()` 已集成状态颜色
- ✅ `setError()` 错误高亮
- 🔜 可选: 添加 Markdown 渲染用于复杂输出

**问题**: 手动处理文本格式

**改造**:
```typescript
// 当前
this.addChild(new Text(`${theme.success('✓')} ${summary}`));

// 改造后
const resultMarkdown = new Markdown(`**${summary}**\nDuration: ${duration}ms`, markdownTheme);
this.addChild(resultMarkdown);
```

---

#### Task 4: working-indicator.ts Loader 组件

**文件**: `src/components/working-indicator.ts`

**当前**: 使用自定义动画

**改造**:
```typescript
import { Loader, type LoaderIndicatorOptions } from '@earendil-works/pi-tui';

const loader = new Loader({
  indicator: 'dots',
  color: 'primary',
});
```

---

### 3.2 中优先级 (P2)

#### Task 5: ask-tool.ts 重构

**文件**: `src/tools/ask/ask-tool.ts`

**问题**: 大量使用 `'═'.repeat(60)` 手动边框

**当前代码**:
```typescript
lines.push('═'.repeat(60));
lines.push(`❓ ${request.question}`);
lines.push('═'.repeat(60));
```

**改造后**:
```typescript
const prompt = new Box([
  new Text(theme.bold(`❓ ${request.question}`)),
  new Text(''),
  ...choices.map(choice => new Text(`  [${choice.value}] ${choice.label}`)),
], 'double');
```

---

#### Task 6: eval-app.ts 边框重构

**文件**: `src/evals/components/eval-app.ts`

**问题**: 使用 `'═'.repeat(70)`, `'─'.repeat(70)` 手动边框

**改造**:
```typescript
// 当前
this.addChild(new Text('═'.repeat(70), 0, 0));

// 改造后
this.addChild(new Box([...content], 'double'));
```

---

#### Task 7: eval-progress.ts 进度条

**文件**: `src/evals/components/eval-progress.ts`

**当前**: 使用 `'█'.repeat()` 手动绘制进度条

**改造方案**: 保留进度条逻辑，但使用 `TruncatedText` 处理溢出

---

#### Task 8: intro.ts Markdown 支持

**文件**: `src/components/intro.ts`

**改造**:
```typescript
import { Markdown } from '@earendil-works/pi-tui';

const welcomeMd = `# Welcome to UpUp
**Version:** ${version}

Start typing your request...`;

this.addChild(new Markdown(welcomeMd, markdownTheme));
```

---

#### Task 9: answer-box.ts Markdown 支持

**文件**: `src/components/answer-box.ts`

**改造**:
```typescript
import { Markdown } from '@earendil-works/pi-tui';

class AnswerBox extends Container {
  private markdown: Markdown;

  constructor(content: string) {
    super();
    this.markdown = new Markdown(content, markdownTheme);
    this.addChild(this.markdown);
  }
}
```

---

### 3.3 低优先级 (P3)

#### Task 10: debug-panel.ts SettingsList

**文件**: `src/components/debug-panel.ts`

**评估**: `SettingsList` 是否适合调试面板

---

#### Task 11: markdown-table.ts 重构

**文件**: `src/utils/markdown-table.ts`

**问题**: 手动绘制表格边框

**改造**: 使用 `Box` 或创建 `Table` 组件

---

#### Task 12: config-tool.ts 缩进处理

**文件**: `src/tools/config-tool.ts`

**问题**: 使用 `' '.repeat()` 手动缩进

**改造**: 评估是否需要特殊处理

---

#### Task 13: skill-tool.ts 分隔线

**文件**: `src/tools/skill-tool.ts`

**问题**: 使用 `'='.repeat(40)` 手动分隔线

**改造**: 使用 `Box` 或 `Spacer`

---

### 3.4 新功能集成 (P4)

#### Task 14: hyperlink() 超链接

**用途**:
- 帮助文档链接
- MCP 服务器文档链接
- 错误消息中的链接

```typescript
import { hyperlink } from '@earendil-works/pi-tui';

new Text(`Docs: ${hyperlink('Click here', 'https://docs.example.com')}`);
```

---

#### Task 15: AutocompleteSuggestions

**用途**: 编辑器自动补全

```typescript
import { AutocompleteSuggestions } from '@earendil-works/pi-tui';

const suggestions = new AutocompleteSuggestions({
  items: [
    { label: '/help', detail: 'Show help' },
    { label: '/clear', detail: 'Clear screen' },
  ],
});
```

---

#### Task 16: setCapabilities() 终端检测

**用途**: 检测终端能力

```typescript
import { detectCapabilities, setCapabilities } from '@earendil-works/pi-tui';

const caps = await detectCapabilities();
setCapabilities({
  hyperlinks: caps.hyperlinks,
  trueColor: caps.trueColor,
  unicode: caps.unicode,
});
```

---

## 四、文件清单

### 4.1 需要修改的文件

```
src/
├── components/
│   ├── BorderBox.ts                 ✅ 新增: 自定义边框组件
│   ├── approval-requests/
│   │   ├── BashApprovalRequest.ts     ✅ 已完成 (BorderBox)
│   │   ├── WriteApprovalRequest.ts    ✅ 已完成 (BorderBox)
│   │   ├── GenericApprovalRequest.ts  ✅ 已完成 (BorderBox)
│   │   └── FullscreenApprovalOverlay.ts ✅ 已完成 (BorderBox)
│   ├── tool-event.ts                 🔄 P1: Markdown
│   ├── working-indicator.ts           🔄 P1: Loader
│   ├── intro.ts                      ✅ 已完成 (BorderBox)
│   ├── answer-box.ts                 ✅ 已完成 (Markdown)
│   └── debug-panel.ts               🔄 P3: SettingsList
├── mcp/
│   └── mcp-ui.ts                    ✅ 已完成 (BorderBox)
├── evals/
│   └── components/
│       ├── eval-app.ts              🔄 P2: Box 重构
│       └── eval-progress.ts         🔄 P2: 进度条优化
├── tools/
│   ├── ask/
│   │   └── ask-tool.ts              🔄 P2: Box 重构
│   ├── config-tool.ts               🔄 P3: 缩进处理
│   └── skill-tool.ts               🔄 P3: 分隔线重构
└── utils/
    └── markdown-table.ts            🔄 P3: 表格组件化
```

### 4.2 已完成

```
src/
├── components/
│   ├── approval-requests/
│   │   └── FullscreenApprovalOverlay.ts  ✅ 已完成
│   └── select-list.ts                  ✅ 已使用 SelectList
├── cli.ts                               ✅ 已使用 TUI
└── session/selector.ts                 ✅ 已使用 pi-tui
```

---

## 五、测试计划

### 5.1 单元测试

```bash
# 运行所有测试
bun test

# 组件测试
bun test src/components/
bun test src/mcp/
bun test src/evals/

# 特定文件测试
bun test src/components/tool-event.test.ts
bun test src/components/approval-requests/
```

### 5.2 手动测试

```bash
# 启动应用
bun start

# 测试场景:
# 1. MCP 服务器管理 → mcp-ui BorderBox ✅
# 2. 授权流程 → approval-requests BorderBox ✅
# 3. 欢迎信息 → intro BorderBox ✅
# 4. AI 回复 → answer-box Markdown ✅
```

---

## 六、风险评估

| 任务 | 风险 | 影响 | 缓解措施 |
|------|------|------|----------|
| BorderBox 组件 | 低 | 中 | 已创建完整单元测试 |
| MCP UI 重构 | 低 | 中 | 已使用相同组件模式 |
| approval-requests 重构 | 低 | 中 | 已使用 BorderBox 替代手动边框 |
| keybindings 类型 | 中 | 中 | 已知问题，待验证 |

# 测试场景:
# 1. MCP 服务器管理 → mcp-ui Box/SelectList
# 2. 授权流程 → approval-requests Box
# 3. 工具执行 → tool-event Markdown
# 4. 加载状态 → working-indicator Loader
# 5. 欢迎信息 → intro Markdown
# 6. AI 回复 → answer-box Markdown
# 7. 评估界面 → eval-app Box
```

---

## 六、风险评估

| 任务 | 风险 | 影响 | 缓解措施 |
|------|------|------|----------|
| MCP UI 重构 | 中 | 中 | 保留原有组件作为 fallback |
| Markdown 迁移 | 中 | 中 | 保留 fallback 渲染 |
| ask-tool 重构 | 中 | 中 | 分步骤测试 |
| Box 边框样式 | 低 | 低 | 使用相同边框风格 |
| 进度条改造 | 低 | 低 | 保持视觉效果 |

---

## 七、时间表

| 任务 | 优先级 | 预计时间 | 累计 |
|------|--------|----------|------|
| Task 1: MCP UI 重构 | P0 | 2-3h | 2-3h |
| Task 2: approval-requests | P1 | 2h | 4-5h |
| Task 3: tool-event Markdown | P1 | 2h | 6-7h |
| Task 4: working-indicator | P1 | 1h | 7-8h |
| Task 5: ask-tool | P2 | 2h | 10h |
| Task 6: eval-app | P2 | 1h | 11h |
| Task 7: eval-progress | P2 | 1h | 12h |
| Task 8: intro | P2 | 30m | 12.5h |
| Task 9: answer-box | P2 | 30m | 13h |
| Task 10-13: 低优先级 | P3 | 2h | 15h |
| Task 14-16: 新功能 | P4 | 3h | 18h |

**总计**: ~18-20 小时

---

## 八、实施策略

### 8.1 分阶段实施

**Phase 1**: MCP UI + approval-requests (P0-P1)
- MCP UI 重构
- approval-requests 剩余组件

**Phase 2**: 核心组件 (P1-P2)
- tool-event Markdown
- working-indicator Loader
- ask-tool 重构
- eval-app 重构

**Phase 3**: 辅助组件 (P2-P3)
- intro/answer-box Markdown
- debug-panel SettingsList
- markdown-table 重构

**Phase 4**: 新功能 (P4)
- hyperlink
- AutocompleteSuggestions
- setCapabilities

### 8.2 改造模式

```typescript
// Before: 手动边框
class OldComponent {
  render(width: number): string[] {
    const lines = [];
    lines.push('┌─ Header ─'.padEnd(width - 1) + '┐');
    lines.push(`│ Content`.padEnd(width - 2) + '│');
    lines.push('└' + '─'.repeat(width - 2) + '┘');
    return lines;
  }
}

// After: pi-tui Box
class NewComponent extends Container {
  constructor() {
    super();
    this.addChild(new Box([
      new Text('Header'),
      new Text('Content'),
    ], 'single'));
  }
}
```

---

## 九、验收标准

### 9.1 功能验收

- [ ] MCP UI 使用 SelectList 和 Box 组件
- [ ] 所有 approval-requests 使用 Box 组件
- [ ] tool-event 使用 Markdown 渲染结果
- [ ] working-indicator 使用 Loader 组件
- [ ] ask-tool 使用 Box 组件
- [ ] eval-app 使用 Box 组件
- [ ] intro/answer-box 使用 Markdown 组件

### 9.2 测试验收

- [ ] 所有现有测试通过
- [ ] 新增组件测试
- [ ] 手动测试覆盖所有场景

### 9.3 代码质量

- [ ] 移除所有手动边框绘制代码
- [ ] 统一的组件使用模式
- [ ] 遵循 pi-tui 最佳实践
- [ ] 代码可读性提升

---

## 十、收益分析

### 10.1 可维护性

- **减少重复代码**: 移除 ~500 行手动边框代码
- **统一风格**: 所有 Box 使用相同的边框风格
- **易于修改**: 边框宽度/样式只需改一处

### 10.2 功能增强

- **Markdown 渲染**: 原生支持代码高亮、链接等
- **自动截断**: TruncatedText 自动处理溢出
- **终端适配**: 自动检测终端能力

### 10.3 用户体验

- **一致的 UI**: 所有面板使用统一的设计语言
- **更好的可读性**: Markdown 渲染更清晰
- **更快的开发**: 新组件直接复用现有模式

---

## 十一、后续计划

### p2.md 完成后

1. **Phase 1**: MCP UI + approval-requests (1-2 天)
2. **Phase 2**: 核心组件重构 (2-3 天)
3. **Phase 3**: 辅助组件 + 新功能 (1-2 天)

### 长期规划

- **p3.md**: 性能优化
- **p4.md**: 新功能规划
- **p5.md**: 国际化支持

---

**计划制定者**: Claude Code 分析
**版本**: v2.0
**分析日期**: 2026-05-21
